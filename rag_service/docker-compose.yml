services:
  rag:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - rag_vectorstore:/app/vectorstore
      - ../backend/seed_data.json:/app/seed_data.json:ro
    environment:
      - CHROMA_PATH=/app/vectorstore

volumes:
  rag_vectorstore:
